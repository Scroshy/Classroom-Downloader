[project]
name = "classroom-downloader"
version = "0.1.0"
description = "A Python tool to automate downloading files, assignments, and materials from Google Classroom."readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-api-python-client>=2.187.0",
    "google-auth-httplib2>=0.3.0",
    "google-auth-oauthlib>=1.2.3",
]
